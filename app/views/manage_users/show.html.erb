<div class="jumbotron">
  <h1 class="display-6">User Details</h1>
  <hr class="my-4">
  <div class="container">
    <div class="row">
        <p class="lead"> 
          <b>Email:</b> <%= @user.email %><br>
        </p>
      </div>
    </div>
  </div>
  <%= link_to 'Back', users_path, class:"btn btn-dark" %> 
  <% if current_user %>
    <%= link_to 'Delete', user_path(@user), method: :delete, data: {confirm: 'Are you sure?'}, class:"btn btn-dark" %>
  <% end %>
</div>
